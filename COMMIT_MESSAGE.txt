feat: implement TravelNest MVP foundation with auth, onboarding, and trips

This commit establishes the core foundation of TravelNest, implementing Phase 0
features including authentication, user onboarding, trip management, and database
schema.

- Restructure project to use src/ directory and update tsconfig paths
- Add Supabase integration with client and server-side authentication
- Create comprehensive database schema with 10+ tables, RLS policies, and migrations
- Implement authentication with login/register pages and auth API endpoints
- Add 30-second onboarding quiz with 4 steps (travel group, vibe, budget, interests)
- Implement trip management API (create, list, update, delete) with unique slugs
- Create trip pages (new, detail, edit) with cover photo support
- Build dashboard page showing upcoming and past trips with member avatars
- Add Zustand stores for auth and onboarding state management
- Add comprehensive documentation (setup guides, database schema, ERD)
- Update dependencies (Supabase, React Query, Zustand, date-fns, framer-motion, lucide-react)
